themedir =  $(datadir)/icons/HighContrast-SVG
size = scalable
context = devices

icondir = $(themedir)/$(size)/$(context)

icon_DATA = \
	drive-removable-media-flash.svg

EXTRA_DIST = \
	$(icon_DATA)
